# Etcha UI Audit — 2026-03-27

## Scope
Full audit of `packages/web/src` covering security, input validation, transaction safety, and UI/UX best practices. Every page, API route, action, and component examined.

---

## Section A: Security — Secrets & Exposure

| # | Finding | Severity | Status |
|---|---------|----------|--------|
| A1 | No hardcoded private keys, mnemonics, or passwords in source | — | PASS |
| A2 | .env.local properly gitignored, not committed | — | PASS |
| A3 | Supabase anon key is NEXT_PUBLIC_ (public by design) | — | PASS |
| A4 | Ergo node URL server-side only — not in client JS bundles (verified in .next/) | — | PASS |
| A5 | localStorage stores only wallet extension name ("nautilus") — no keys/tokens | — | PASS |
| A6 | Wallet integration follows EIP-12 standard, no private keys stored | — | PASS |
| A7 | Console.log statements log partial EIP-12 TX JSON (portfolio lines 642, 1054) | MEDIUM | OPEN — keeping for Gold lifecycle test, remove after |
| A8 | Console.log in exercise flow logs token IDs and oracle indices (lines 900-974) | MEDIUM | OPEN — keeping for Gold lifecycle test, remove after |

---

## Section B: Input Validation & XSS

| # | Finding | Severity | Status |
|---|---------|----------|--------|
| B1 | /api/my-boxes ecPoint — length-checked but not hex-validated | MEDIUM | FIXED — regex hex validation added |
| B2 | /api/boxes address — no validation, passed directly to node | HIGH | FIXED — length bounds (30-120) added |
| B3 | /api/box-by-id boxId — length-checked but not hex-validated | MEDIUM | FIXED — regex hex validation added |
| B4 | /api/spot index — parseInt allows "20abc" to parse as 20 | MEDIUM | FIXED — strict digit-only regex |
| B5 | /api/submit — no size limit or structure validation on POST body | LOW | FIXED — structure check + 500KB limit |
| B6 | /api/poll contractAddress — no format validation | MEDIUM | FIXED — hex-only regex added |
| B7 | Option names from R4 register rendered without sanitization | HIGH | FIXED — strip non-alphanumeric, truncate to 100 chars (both my-boxes and activity routes) |
| B8 | Activity descriptions from chain data unbounded in length | MEDIUM | FIXED — covered by R4 name sanitization |
| B9 | Write page strike price — no bounds checking (negative, zero, 1e308) | MEDIUM | FIXED — must be > 0 and < 1e12 |
| B10 | Write page contract size — can be negative via devtools | HIGH | FIXED — must be > 0 and < 1e10 |
| B11 | Number→BigInt conversions lack overflow guards | MEDIUM | OPEN — bounds checks on strike/contractSize catch most cases; full safeNumberToBigInt helper deferred |
| B12 | No dangerouslySetInnerHTML usage found | — | PASS |

---

## Section C: Transaction Safety

| # | Finding | Severity | Status |
|---|---------|----------|--------|
| C1 | Write button not disabled during async execution — double-click risk | MEDIUM | FIXED — disabled when step > 0 |
| C2 | Exercise button not disabled during submission | MEDIUM | PASS — already had `disabled={!canExercise \|\| isSubmitting}` |
| C3 | Fee address (DAPP_UI_FEE_TREE_HEX) hardcoded without runtime validation | HIGH | FIXED — P2PK format check at module load |
| C4 | Integer truncation in exercise strike payment (BigInt division rounds down) | — | CLOSED — matches on-chain contract (ErgoScript also truncates). Contract uses >= check so amounts align. |
| C5 | Close TX had OUTPUTS.size == 3 (wallet boxes created extra change output) | CRITICAL | FIXED (earlier today) — now uses only reserve box, OUTPUTS.size == 2 |
| C6 | Close TX assumes issuer = wallet caller | MEDIUM | FIXED — now extracts issuer from R9 EC point |
| C7 | Buy flow — premium not re-validated after fetching fresh box from node | MEDIUM | OPEN — race condition if order partially filled between render and TX. Contract rejects mismatched premium so no fund loss, just failed TX. |
| C8 | No retry mechanism for node-rejected TXs after signing | MEDIUM | OPEN — signed TX is lost on node error. User must restart flow. |
| C9 | ERG vs token collateral calculation uses different fee multiples (3x vs 4x) | LOW | FIXED — fee schedule documented in create-option.ts |
| C10 | List for sale — register values set correctly | — | PASS |
| C11 | Cancel sell order — tokens return to correct address | — | PASS |
| C12 | Buy — correct stablecoin, correct token amounts | — | PASS |
| C13 | Close — works for both ERG calls and token-collateral options | — | PASS |
| C14 | Exercise — stablecoin read from R8 (not user input) | — | PASS |

---

## Section D: UI/UX & Best Practices

| # | Finding | Severity | Status |
|---|---------|----------|--------|
| D1 | Footer links all dead (href="#") — Docs, GitHub, Discord, Telegram, X | HIGH | FIXED — replaced with Learn, GitHub, Telegram (real URLs) |
| D2 | No React error boundary — component crash shows white screen | MEDIUM | FIXED — ErrorBoundary wrapping all /app/* pages |
| D3 | No page metadata on /app/* pages (bad for SEO, browser tabs) | MEDIUM | FIXED — Dashboard, Market get titles; layout template "%s — Etcha" |
| D4 | Uses browser alert() for validation errors (write page, wallet button) | MEDIUM | OPEN — replace with toast/inline errors after beta |
| D5 | Navbar not mobile-responsive — no hamburger menu | MEDIUM | OPEN |
| D6 | Landing page CSS has only 1 @media query — breaks on mobile | MEDIUM | OPEN |
| D7 | Portfolio page silently swallows API errors — no user feedback | MEDIUM | FIXED — error banner with retry button |
| D8 | No skeleton loaders — text "Loading..." instead of visual indicators | LOW | OPEN |
| D9 | StatsBar loading state not visually distinct | LOW | OPEN |
| D10 | No focus-visible styles on interactive elements | LOW | OPEN |
| D11 | Market page has no pagination | LOW | OPEN — acceptable for current volume |
| D12 | Accessibility: no aria-labels on interactive elements | HIGH | FIXED — aria-label, aria-expanded, aria-pressed, role="dialog", role="menu", role="tooltip" added across Navbar, WalletButton, Tooltip, TxStatus, TradePanel, ExerciseDialog, ListForSaleModal, pagination |
| D13 | Accessibility: no focus trap in modals | MEDIUM | OPEN |
| D14 | Accessibility: dropdown menus not keyboard-navigable | MEDIUM | OPEN |
| D15 | Trade page has no dynamic page metadata (per asset) | LOW | FIXED — generateMetadata shows "Trade ETH Options — Etcha" etc. |

---

## Summary

| Category | Total | Passed/Closed | Fixed | Open |
|----------|-------|---------------|-------|------|
| A: Security | 8 | 6 | 0 | 2 (debug logging — intentional hold) |
| B: Input Validation | 12 | 1 | 10 | 1 |
| C: TX Safety | 14 | 6 | 5 | 3 |
| D: UI/UX | 15 | 0 | 6 | 9 |
| **Total** | **49** | **13** | **21** | **15** |

---

## Open Items by Priority

### Immediate (before public beta)
- **A7/A8**: Remove debug console.log after Gold lifecycle test

### Short-term
- **B11**: SafeNumberToBigInt helper for overflow protection
- **C7**: Re-validate sell order premium from fresh node box before buy TX
- **C8**: TX retry mechanism (store signed TX, offer resubmit)
- **D4**: Replace alert() with toast notifications
- **D13/D14**: Modal focus trap + keyboard nav for dropdowns

### Longer-term
- **D5/D6**: Mobile responsive navbar + landing page
- **D8/D9/D10**: Skeleton loaders, focus styles, loading indicators

---

## Audit Metadata
- **Date**: 2026-03-27
- **Branch**: ui-polish-beta
- **Auditor**: Claude Code (automated)
- **Scope**: packages/web/src — all pages, API routes, TX builders, components
- **Method**: Static analysis + code review (no runtime/penetration testing)
